# HANDOFF-PROMPT — سند انتقال پروژه «Bazino»

> **هدف این سند:** انتقال کامل وضعیت پروژه به هر عامل/شریک توسعه‌ای آینده (انسان یا هوش مصنوعی): قوانین مالک پروژه، تصمیم‌های محصولی، کارهای انجام‌شده با اشاره به اسناد، و کارهای باقی‌مانده.
> **آخرین به‌روزرسانی:** ۲۰۲۶-۰۹-۰۵ · **شاخه کاری:** `arena/01a070c3-bazino-educational-game`

---

## 📜 قوانین پروژه (تعریف‌شده توسط مالک پروژه — اجرا الزامی است)

### قانون ۱ — قانون «پلن ← تأیید ← اجرا» ✅ (ثبت: ۲۰۲۶-۰۹-۰۵)
برای **هر** درخواست مالک پروژه:
1. ابتدا **پلن** ارائه شود (بدون هیچ تغییری در فایل‌ها؛ خواندن/جستجو مجاز است).
2. اجرا فقط پس از تأیید پلن با کلمهٔ **«شروع کن»**.
3. بدون «شروع کن»، هیچ فایلی ساخته/ویرایش/حذف نمی‌شود.

> ⚖️ **اصلاحیهٔ قانون ۱ (۲۰۲۶-۰۹-۰۵ — دستور مالک: «دیگر نیازی به ارائهٔ پلن نیست؛ کلیهٔ فازها را به‌طور بی‌وقفه انجام بده»):**
> **حالت اجرای پیوسته (Continuous Build Mode)** فعال شد:
> - برای **فازهای ۲ تا ۶ نقشهٔ راه GDD** دیگر پلن/تأیید میان‌فازی لازم نیست؛ اجرا بدون وقفه تا پایان ادامه می‌یابد.
> - مواردی که ذاتاً خارج از اختیار عامل است (مدارک ثبت شرکت/بانکی برای فعال‌سازی **واقعی** Sanal POS، کلید امضای انتشار APK، برگزاری تورنمنت فیزیکی، بازبینی حقوقی محلی) = **«بلاکر مالک»** ثبت می‌شود و **بقیهٔ کارها را متوقف نمی‌کند** (به‌جای آن stubs/sandboxes/مستندات تحویل می‌شود).
> - بعد از هر فاز، این سند همچنان **به‌روزرسانی می‌شود** و پیشرفت به مالک گزارش داده می‌ماند.
> - قانون ۱ برای درخواست‌های *جدیدِ خارج از نقشهٔ راه فعلی* همچنان ساری است.

> ⚠️ هر قانون جدید مالک در همین بخش به‌صورت شماره‌دار ثبت و فهرست اصلاحیه‌ها نگه‌داری می‌شود.


### قانون ۲ — «جذابیت مقدم بر آموزش» (ثبت: ۲۰۲۶-۰۹-۰۵ — از گفت‌وگوی طراحی)
در طراحی بازی‌های اکشن این مخزن: لذت بازی هرگز فدای آموزش نمی‌شود؛ بازیکن نباید احساس یادگیریِ درسی کند (قرارداد کامل در GDD §۱.۱ — `design/ACTION-ADVENTURE-GDD.md`).

### قانون ۳ — معمای هر منطقه = باس پایانی آن (ثبت: ۲۰۲۶-۰۹-۰۵)
ساختار هر منطقه: موانع فیزیکی + نبرد + کاوش در مسیر (با گنجینه‌های «فرمول/اطلاعات» که عملاً لازم‌اند) ← **معمای اصلی هر منطقه همان غول/باس پایانی است**.

---

## 🎮 تصمیم‌های محصولی ثبت‌شده (REV 4 — مرجع قطعی: رجیستری D در GDD)

**بازی اصلی: اکشن-ادونچر دوبعدی «Bazino» (به‌غیر از بازی کوییزی قبلی که مستقل است)**

| # | تصمیم |
|---|---|
| D1 | سبک: ۲D به الگوی Hollow Knight؛ نه ۳D |
| D2 | Godot 4 → خروجی اندروید (APK) + PC (ویندوز/لینوکس)؛ تحت وب نیست |
| D3 | ۷ منطقه درسی = ریاضی (همه شاخه‌ها)، فیزیک، شیمی، زیست‌شناسی، انگلیسی، ICT/فناوری، ذکا/منطق — به‌جز ادبیات/تاریخ/جغرافیا/مذهبی |
| D4 | زبان TR پیش‌فرض + EN + FA |
| D5 | سطح‌بندی بر اساس پایه تحصیلی ۶–۱۲ (انتخاب در شروع) |
| D6 | گیم‌پلی: کاوش+موانع+نبرد ← باس=معمای پایانی؛ گنجینهٔ فرمول لازم |
| D7 | (همان قانون ۲) |
| D8 | ارزیابی پنهان پیوسته + موتور تسلط سروری (BKT v1)؛ تکرار تطبیقی مباحث ضعیف تا «ملکه ذهن»؛ دروازهٔ تسلط در فینال درس |
| D9 | جوایز فقط غیرنقدی (کوپن/ساعت رایگان میانه؛ دوچرخه/PS5 فینال فصل) |
| D10 | اپ والدین Flutter: داشبورد درسی + توصیه یاری + Reality Bridge (ثبت نمرات واقعی ← بوف) + کیف پول |
| D11 | پرداخت: مرحله ۱ رایگان؛ شارژ **حضوری** در گیم‌نت؛ لایه Sanal POS (İktisat Bank Cardplus / Near East Bank) آماده ولی غیرفعال تا فعال‌سازی |
| D12 | گیم‌نت: مرحله هفتگی گروهی (حالت سالن) + فینال فصل فقط در گیم‌نت |
| D13 | دامنهٔ فاز ۱ = **فصل ۱ (نیمسال Güz)** بازی؛ پایه‌های ۶–۸ گروه پرتاب؛ `game/` (کوییز قبلی) دست‌نخورده و مستقل |

📖 سند کامل: [`design/ACTION-ADVENTURE-GDD.md`](design/ACTION-ADVENTURE-GDD.md)

---

## ✅ کارهای انجام‌شده (با اشاره به اسناد)

### ۱. پژوهش نظام آموزشی قبرس شمالی (۱۲ → پیش‌دانشگاهی) — درخواست اصلی ✅
پوشهٔ **`research/`** — ۸ سند فارسی:
| سند | محتوا |
|---|---|
| `research/README.md` | فهرست + خلاصهٔ مدیریتی |
| `research/01-education-system-overview.md` | ساختار ۵+۳+۴، اجبار ۶–۱۵، نهادها |
| `research/02-curriculum-middle-school.md` | Ortaokul ۶–۸ + سرفصل‌های رسمی پایه ۸ (کتاب‌چهٔ آزمون ۲۰۲۶) |
| `research/03-curriculum-high-school.md` | Lise ۹–۱۲، کالج‌ها، IGCSE/A-Level |
| `research/04-exams-and-pathways.md` | KGS-1/KGS-2/Yerleştirme (۱۲۵ سؤال/حدنصاب ۶۰/+۵٪)، دانشگاه |
| `research/05-audience-and-needs.md` | پرسوناها و نیازها |
| `research/06-game-design-spec.md` | GDD بازیِ کوییزی (Qu/ History — مربوط به پروژهٔ مستقل `game/`) |
| `research/07-sources.md` | منابع + موارد باز راستی‌آزمایی |

### ۲. چرخهٔ تصمیم‌گیری طراحی بازی اکشن (جمع‌بندی با مالک) ✅
- ۷ سؤال طراحی (دروس، موتور، دامنه ریاضی، سرنوشت `game/`، سطح‌بندی، دید، دامنهٔ اولیه) با پاسخ کلیکی/سفارشی مالک ثبت شد.
- بازبینی‌های بعدی مالک: ۳D ← ۲D (Hollow Knight)، جوایز نقدی ← **غیرنقدی**، افزودن: ارزیابی سروری، اپ والدین، مدل گیم‌نت، فصل‌بندی مطابق سال تحصیلی KKTC، پرداخت حضوری در گیم‌نت + پژوهش درگاه Sanal POS بانک‌های محلی (ثبت در پیوست GDD).
- خروجی: **GDD REV4** در `design/ACTION-ADVENTURE-GDD.md` (فاز ۱ تحویل شد).

### ۳. نمونهٔ کوییزی `game/` — ⚠️ مستقل و دست‌نخورده
MVP وب سه‌زبانه قبلی در `game/` + تصویر `game/assets/hero-map.png` + `README.md` ریشه/بازی. طبق D13 این پروژه مستقل می‌ماند و بخشی از بازی اکشن نیست.

---

### ۴. فاز ۲ — بک‌اند v1 + سرفصل‌های فصل ۱ ✅ (۲۰۲۶-۰۹-۰۵، در حالت اجرای پیوسته)
- **`server/`**: FastAPI کامل — auth(JWT)/لینک خانواده/تله‌متری/موتور تسلط BKT-سبک (نیم‌عمر ۱۴روزه، آستانه ۷۰)/adaptive stage config/حالت سالن (کد ۶رقمی + QR)/کیف‌پول حضوری Idempotent + رسید HMAC/کاتالوگ جوایز غیرنقدی/Reality Bridge/پنل ادمین + اسکلت Alembic + `BankGatewayStub` خاموش.
- ✅ **۱۶ تست pytest سبز** + دموی E2E (`scripts/e2e_demo.py`) کامل اجرا شد (تسلط→سبز، شارژ Idempotent، Venue، بوف).
- **`curriculum/`**: ۳۶ موضوع فصل ۱ (پایه ۸ از کتاب‌چه رسمی؛ ۶–۷ تخمینی علامت‌گذاری‌شده) + SOURCES.md + extract_programs.py.
- ⚠️ تخصیص نیمسالی = هنوز «تخمین رسمی» (`semester_estimate:true`) تا دانلود PDFهای TTD — در فاز ۶ نهایی می‌شود.

### ۵. فاز ۳ — کلاینت Godot v1 ✅ (۲۰۲۶-۰۹-۰۵)
- **`godot/`**: Godot 4.3 (gl_compatibility، autorun نقشه‌کلید runtime): Player با حس HK (شتاب/کایوت/بافر/دش i-frame/کامبو/هیت‌استاپ)، دوربین lookahead، دشمن wisp، MathGate (پاسخ=ایستادن روی سکو — بدون پنجره سؤال)، Shrine، FormulaScroll (دفترچه فرمول=توان)، **باس EskiHesapci** (۳ فاز سپر-معادله) + StageBuilder (پس‌زمینه سیلوئت کد-محور) + HUD/منو (TR/EN/FA، پایه ۶–۸، کد سالن) + ۳ مرحلهٔ محتوای واقعی پایه ۸ (EBOB/EKOK، توان، رادیکال، اتحاد) + نسخهٔ سبک ۶–۷.
- **`tools/godot_lint.py`** + پارسر واقعی **gdtoolkit 4.3.3** → هر ۱۵ فایل .gd معتبر. ⚠️ باینری Godot در sandbox قابل دانلود نبود (CDN) → اجرای واقعی روی دستگاه توسعه (README).
- ⚠️ قاعدهٔ فنی بک‌اند: برای کلاینت `GET /auth/me/player` اضافه شد (child_id برای تله‌متری).

## ⏳ کارهای باقی‌مانده (حالت اجرای پیوسته — بدون پلن میان‌فازی)

| # | فاز | کار | مرجع |
|---|---|---|---|
### ۶. فاز ۴ — اپ والدین Flutter ✅ (۲۰۲۶-۰۹-۰۵)
- **`parents_app/`** (Flutter، وابستگی سبک): احراز/ثبت والد، کد خانواده QR (BZ-FAMILY)، داشبورد تسلط موضوعی + چراغ آمادگی امتحان فصل، توصیه‌های یاری خانگی از موضوعات ضعیف، **Reality Bridge** (ثبت نمره←بوف)، کیف پول حضوری با QR `BZ-TOPUP:{parent_id}` + دکمهٔ درگاه «به‌زودی» (غیرفعال تا مدارک بانک).
- ⚠️ SDK دارت/فلاتر در sandbox نیست → با بازبینی ایستای دقیق تحویل شد؛ تست‌های مدل در `test/widget_test.dart`.

## ⏳ کارهای باقی‌مانده (حالت اجرای پیوسته — بدون پلن میان‌فازی)

| # | فاز | کار | مرجع |
|---|---|---|---|
### ۷. فاز ۵ — کو-اوپ، تورنمنت، مناطق ۲و۳ ✅ (۲۰۲۶-۰۹-۰۵)
- **بک‌اند**: روتر `/tournament/eligibility/{child}` (حداقل ۳ درس تسلط ≥۷۰) + تست.
- **کلاینت**: باسِ عمومی‌شدهٔ ناحیه‌ای (پیکربندی name/topic/color — پایهٔ همهٔ ۷ باس)، موانع تازه: **LeverScale** (اهرم با فیزیک واقعی RigidBody2D+PinJoint2D) و **CircuitGate** (الگوی منطق)، مناطق **Fizik Kalesi** و **Dil Limanı** (محتوای پایه ۸ Güz: ماشین‌های ساده، نور/صدا، quantifiers/can-have to/adverbs — انگلیسی بدون ترجمه)، مراحل **هفتگی W1–W4** (قفل Venue)، **فینال فصل** (Boss Rush سه‌باسی + شرط ۳ منطقه + جایزهٔ grand با کد تحویل در باجه)، **CoopSession** (ENet LAN، همگام‌سازی موقعیت بازیکن دوم).
- تأیید: lint + gdparse هر ۱۸ فایل، ۱۸ تست بک‌اند سبز. ⚠️ تست میدانی واقعی در گیم‌نت = نیازمند اجرای فیزیکی (بلاکر محیط، نه کد).

### ۸. فاز ۶ — کامل‌سازی اکوسیستم ✅ (۲۰۲۶-۰۹-۰۵)
- **۷/۷ منطقه در سیستم**: دادهٔ مرحله + باس برای زیست (İstilacı)، شیمی (pH Canavarı)، ICT (Ghost Virus)، منطق (Saatçi) با همان کلاس باس عمومی + ۴ کلید i18n/دکمهٔ منطقه در منو (دکمهٔ «فصل ۲» غیرفعال-teaser).
- **curriculum**: `kktc-season2-grades6-8.json` (۱۳ موضوع Bahar: مکمل‌های رسمی پایه ۸ + ICT/ذکا) + `kktc-grades9-12-draft.json` (۱۸ موضوع Lise)؛ ✅ **ttd.mebnet.net دسترسی‌پذیر و فهرست برنامه‌ها تأیید شد** (Temel Eğitim/Genel Ortaöğretim/Örnek Yıllık Planlar برای پخش نیمسال) — ثبت در SOURCES.md؛ استخراج نهایی PDF = کار توسعهٔ میدانی.
- **اقتصاد**: endpoint `/wallet/gateway_status` (وضعیت درگاه برای اپ) + README ریشهٔ بازنویسی‌شدهٔ کل اکوسیستم.
- تأیید نهایی: lint+gdparse همهٔ ۱۸ .gd + ۱۰ JSON، ۱۸ تست بک‌اند، E2E سبز.

## 🏁 وضعیت: همهٔ فازهای ۱–۶ اجرا شد. باقی‌مانده = فقط «بلاکرهای مالک» (خارج از کد):

### ۹. موج تکمیلی («شروع کن» سوم، ۲۰۲۶-۰۹-۰۵) ✅
- **استخراج رسمی**: تلاش برای PDFهای ttd — دسترسی curl به دامنه مسدود (TLS) و پیوندهای اسناد داخل iframe صفحه‌ساز WP پنهان‌اند → استخراج = کار میدانی روی دستگاه توسعه (rdy: extract_programs.py + فهرست‌های تأییدشده در SOURCES.md).
- **فصل ۲ در بازی**: ۵ فایل `stages_*_s2.json` (ریاضی: خطی/شیب/مثلث/احتمال — فیزیک: انرژی/آب‌وهوا — انگلیسی: past/comparison/future — زیست: میتوز/میوز/وراثت — شیمی: ماده و گرما/اسید-باز) + کلید **① Güz / ② Bahar** در منو (ict/logic فعلاً فقط Güz — سقوط‌به‌فصل۱).
- **پنل باجهٔ سالن**: `/admin/ui` (HTML/JS سبک) — ورود پرسنل، شارژ حضوری با رسید، صدور کد روزانه + تصویر QR، فهرست کدهای تحویل جوایز و تراکنش‌ها.
- **CI**: `.github/workflows/ci.yml` — بک‌اند pytest + godot lint/gdparse + یکپارچگی JSONها.
- اعتبار نهایی: ۱۸ تست، lint، gdparse، `/admin/ui` + `/health` سبز.

| # | بلاکر مالک | اقدام لازم |
|---|---|---|
| ۱ | پرداخت آنلاین واقعی | مدارک ثبت شرکت + قرارداد اجاره + درخواست به İktisat Bank (Cardplus) یا Near East Bank ← سپس `GATEWAY_PROVIDER` عوض و stub واقعی تکمیل می‌شود (GDD §۹.۳) |
| ۲ | تست تعاملی بازی/اپ | نصب Godot 4.3+ و Flutter SDK روی دستگاه ↔ اجرای `godot/` و `parents_app/` (sandbox: دانلود باینری مسدود بود — کد با gdparse/gdlint 4.3.3 تأیید شد) |
| ۳ | پخش رسمی نیمسال | دانلود PDF تب‌های ttd.mebnet.net + اجرای `curriculum/extract_programs.py` ← `semester_estimate:false` (مسیرها در SOURCES.md) |
| ۴ | تورنمنت فیزیکی اول | بازبینی حقوقی محلی جوایز/سن + رضایت والدین + برنامهٔ سالن |
| ۵ | پرسش‌های باز پژوهشی | `research/07-sources.md` (Garanti Sınavları، آمار رسمی) |

> 📌 درخواست‌های **جدید** (خارج از نقشهٔ راه فعلی) طبق قانون ۱: پلن ← «شروع کن» ← اجرا.

---

## 🧭 دستورالعمل‌های حاکمیتی برای عامل بعدی
- زبان ارتباط با مالک: **فارسی**؛ اسناد فارسی می‌مانند (اصطلاحات رسمی ترکی/انگلیسی حفظ می‌شود).
- هر تغییر فقط طبق **قانون ۱** (پلن ← «شروع کن» ← اجرا).
- پس از هر تحویل، این سند به‌روزرسانی می‌شود (قوانین/انجام‌شده/باقی‌مانده).
- شاخهٔ کاری فقط: `arena/01a070c3-bazino-educational-game`؛ شاخهٔ جدید ساخته نمی‌شود.
- `game/`، `research/01..07` محصولات نهایی‌شده‌اند؛ ویرایش‌شان فقط با دستور صریح مالک.
- رجیستری D در GDD مرجع قطعی تصمیم‌های طراحی است؛ این سند خلاصه‌اش را نگه می‌دارد.
